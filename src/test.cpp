#include <Eigen/Dense>
#include "ndarray"
#include <stdio>

typedef Eigen::Array<int,3,1> int3;	//used for triangle geometry computations


int main(){
    int_2 arr(3, 10);
    f[0,0] = -1;
    printf(f[0,0])
    return f[0,0];
}